describe('MyComponent', () =>{
    it('Should be true', () =>{
        const foo = true;
        expect(foo).toBe(true);
    });
    it('Should be true', () =>{
        const foo = true;
        expect(foo).toBe(false);
    });
})